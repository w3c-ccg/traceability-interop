description: Not Found
content:
  application/json:
    schema:
      allOf:
        - $ref: '../schemas/Error.yml'
        - type: object
          properties:
            code:
              enum: [404]
            message:
              enum: ["Not Found: The requested resource could not be found"]
