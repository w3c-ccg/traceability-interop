description: Not Found
content:
  application/json:
    schema:
      allOf:
        - $ref: '../schemas/Error.yml'
        - type: object
          properties:
            message:
              enum: ["Not Found: The requested resource could not be found"]
