ttile: Presentation Options
type: object
description: JSON-LD Verifiable Presentation Options
properties:
  verificationMethod:
    type: string
    description: Verification Method used to verify proof.
  domain:
    type: string
    description: The domain for the presentation
  challenge:
    type: string
    description: The challenge for the presentation
example:
  {
    "verificationMethod": "did:example:123#key-2",
    "domain": "example.com",
    "challenge": "123",
  }
